import { AutomaticGetDataByUrlDirective } from './automatic-get-data-by-url.directive';

describe('AutomaticGetDataByUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new AutomaticGetDataByUrlDirective();
    expect(directive).toBeTruthy();
  });
});
